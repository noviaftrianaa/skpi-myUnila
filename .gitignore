apps_pdpt/storage/api-docs/api-docs.json
.idea/dbnavigator.xml
.idea/modules.xml
.idea/pdut-unila.iml
.idea/php.xml
.idea/vcs.xml
.idea/inspectionProfiles/Project_Default.xml
.idea/jsLinters/jshint.xml
dashboard/.env-mp
dashboard/.env

# Claude Code settings (local only)
.claude/
.mcp.json

# VSCode workspace settings (local only)
.vscode/

# Docker compose development files with credentials
docker-compose.dev.yml
docker-compose.*.local.yml

# Production environment files (contains secrets)
deployment/production/**/.env
deployment/production/**/.env.local
deployment/production/**/.env.production

# Local development environment files (contains credentials)
deployment/local/.env
deployment/local/services/**/.env
frontend/.env.local

# SIMPEDAM proxy files (local tools)
deployment/local/SIMPEDAM_PROXY_SETUP.md
deployment/local/simpedam-proxy.js
deployment/local/start-simpedam-proxy.bat

# Keep .env.example files for reference
!deployment/production/**/.env.example

# Allow encrypted env files (SOPS)
!deployment/production/**/.env.encrypted

# Temporary files
*.tmp
*.log
.DS_Store

# Documentation folders - ignore summary and sensitive security reports
docs/summary/
docs/security/

# Monitoring GSC secrets
deployment/local/secrets/

# Local scratch/ignore folder (reports, data exports, helper scripts)
docs/ignore/

# Local presentasi progress folder (PPTX generators + reports for pimpinan)
docs/presentasi-progress-2026/

# Personal/internal docs (sensitive, generated reports, personal notes) — gitignored
docs/_internal/

# Historical Claude planning docs (renamed from docs/claude/) — gitignored
docs/_archive/

# Local backups (Kong config dumps etc) — generated by cron on VM1
backups/

# Monitoring & internal deployment plans (local only)
deployment/docs/

# Local utility tools (md2pdf, foto-mhs-uploader, dll) — personal, jangan commit
docs/tools/

.env
*.bak-*
*.env.bak*
backend/project-service/.env

# Large MySQL dumps (jangan commit ke git, sync via scp/rsync)
data-model/script/mysql/*_dump.sql
data-model/script/mysql/kkn_new_banget.sql
