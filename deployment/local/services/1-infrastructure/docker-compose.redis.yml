version: '3.8'

services:
  redis:
    image: redis:7-alpine
    container_name: myunila-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - myunila-network
    command: redis-server --appendonly yes
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

networks:
  myunila-network:
    driver: bridge
    name: myunila-network

volumes:
  redis_data:
    driver: local
    name: myunila-redis-data
